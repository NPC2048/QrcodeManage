server:
  port: 8091
spring:
  # 数据源配置
  datasource:
      url: jdbc:mysql://139.159.236.186/2qrcode?pinGlobalTxToPhysicalConnection=true&autoReconnect=true&useUnicode=true&serverTimezone=GMT%2b8&useSSL=false&characterEncoding=utf-8
      username: root
      password: PasswordHuaWeiYun123
      hikari:
        # 最小连接数
        minimum-idle: 3
        # 最大连接数
        maximum-pool-size: 20
        # 允许闲置的最长时间 ms
        idle-timeout: 30000
        # 池中连接最长生命周期
        max-lifetime: 180000
# mybatis-plus 配置
mybatis-plus:
  configuration:
    # 开发环境, 打印 sql 到控制台
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
debug: false
